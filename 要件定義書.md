# Minecraft Community Mod 要件定義書

## 1. 概要

Minecraftサーバーのプレイヤー参加・退出、サーバー起動・停止などを検知し、Minecraft Community APIへ送信するサーバーサイドModを開発する。

DiscordSRVやSDLinkなど既存のMinecraft・Discord連携Modの設計や実装を参考にし、Minecraftサーバーへの負荷を抑える。

独自機能を必要以上にMinecraft側へ持たせず、Minecraft Modはイベント取得とAPI通信を担当する薄い構成とする。

## 2. 対応環境

以下の2環境に対応する。

```text
Minecraft 1.20.1
Forge

Minecraft 1.21.1
NeoForge
```

配布jarは環境ごとに分ける。

```text
mc-community-forge-1.20.1-x.x.x.jar

mc-community-neoforge-1.21.1-x.x.x.jar
```

## 3. GitHubリポジトリ

Forge版とNeoForge版でGitHubリポジトリを分けない。

1つのリポジトリで管理する。

```text
mc-community-mod
```

APIは別リポジトリとする。

```text
mc-community-api
mc-community-mod
```

## 4. プロジェクト構成

Gradle Multi-Project構成を採用する。

```text
mc-community-mod/
├─ common/
│  ├─ build.gradle
│  │
│  └─ src/
│     └─ main/
│        └─ java/
│           └─ com/example/mccommunity/
│              ├─ api/
│              │  ├─ ApiClient.java
│              │  └─ ApiException.java
│              │
│              ├─ model/
│              │  ├─ PlayerInfo.java
│              │  ├─ JoinEventRequest.java
│              │  ├─ LeaveEventRequest.java
│              │  ├─ HeartbeatRequest.java
│              │  └─ LinkCodeResponse.java
│              │
│              ├─ service/
│              │  ├─ CommunityService.java
│              │  ├─ HeartbeatService.java
│              │  └─ LinkService.java
│              │
│              └─ util/
│                 └─ JsonUtil.java
│
├─ forge-1.20.1/
│  ├─ build.gradle
│  │
│  └─ src/
│     └─ main/
│        ├─ java/
│        │  └─ com/example/mccommunity/forge/
│        │     ├─ MinecraftCommunityForge.java
│        │     │
│        │     ├─ event/
│        │     │  ├─ PlayerEventHandler.java
│        │     │  └─ ServerEventHandler.java
│        │     │
│        │     ├─ command/
│        │     │  └─ DiscordCommand.java
│        │     │
│        │     └─ config/
│        │        └─ ForgeConfig.java
│        │
│        └─ resources/
│           ├─ META-INF/
│           │  └─ mods.toml
│           └─ pack.mcmeta
│
├─ neoforge-1.21.1/
│  ├─ build.gradle
│  │
│  └─ src/
│     └─ main/
│        ├─ java/
│        │  └─ com/example/mccommunity/neoforge/
│        │     ├─ MinecraftCommunityNeoForge.java
│        │     │
│        │     ├─ event/
│        │     │  ├─ PlayerEventHandler.java
│        │     │  └─ ServerEventHandler.java
│        │     │
│        │     ├─ command/
│        │     │  └─ DiscordCommand.java
│        │     │
│        │     └─ config/
│        │        └─ NeoForgeConfig.java
│        │
│        └─ resources/
│           └─ META-INF/
│
├─ gradle/
├─ build.gradle
├─ settings.gradle
├─ gradle.properties
├─ gradlew
├─ gradlew.bat
└─ README.md
```

## 5. commonの責務

MinecraftバージョンやMod Loaderに依存しない処理を配置する。

主に以下を担当する。

```text
HTTP通信
API Request / Response DTO
非同期処理
JSON変換
Heartbeatロジック
APIエラー処理
共通サービス
```

可能な限りMinecraft固有クラスをcommonへ持ち込まない。

例えば以下のような独自DTOへ変換する。

```java
public record PlayerInfo(
    UUID uuid,
    String name,
    String serverId
) {}
```

Forge・NeoForge固有の ServerPlayer などをAPI通信層まで渡さない。

## 6. Forge 1.20.1の責務

Forge固有APIとの接続のみを担当する。

```text
Player Joinイベント取得
Player Leaveイベント取得
Server Startイベント取得
Server Stopイベント取得
コマンド登録
Config読み込み
ServerPlayer → PlayerInfo変換
```

イベント取得後はcommonへ処理を渡す。

## 7. NeoForge 1.21.1の責務

NeoForge固有APIとの接続のみを担当する。

```text
Player Joinイベント取得
Player Leaveイベント取得
Server Startイベント取得
Server Stopイベント取得
コマンド登録
Config読み込み
ServerPlayer → PlayerInfo変換
```

Forge版と同じ共通サービスを呼び出す。

## 8. Player Join

プレイヤー参加時にLoader固有イベントを取得する。

プレイヤーから以下を取得する。

```text
Minecraft UUID
Minecraft Name
Server ID
```

commonへ渡す。

```java
CommunityService.onPlayerJoin(playerInfo);
```

commonからAPIへ以下を送信する。

```text
POST /v1/minecraft/events/join
```

HTTP通信はMinecraftメインスレッド上で待機しない。

## 9. Player Leave

プレイヤー退出時に以下を送信する。

```text
POST /v1/minecraft/events/leave
```

送信情報は以下とする。

```text
Minecraft UUID
Minecraft Name
Server ID
```

## 10. Server Start

Minecraftサーバー起動完了時に以下を送信する。

```text
POST /v1/minecraft/events/server-start
```

API側でクラッシュによって残った未終了セッションを修復するために使用する。

## 11. Server Stop

Minecraftサーバーの正常停止時に以下を送信する。

```text
POST /v1/minecraft/events/server-stop
```

API側でオンライン中プレイヤーのセッションを正常終了するために使用する。

## 12. Heartbeat

Minecraftサーバー起動中は5分に1回Heartbeatを送信する。

```text
POST /v1/minecraft/heartbeat
```

送信内容は以下とする。

```json
{
  "serverId": "oceanblock2",
  "players": [
    "uuid-a",
    "uuid-b",
    "uuid-c"
  ]
}
```

HeartbeatはMinecraft TickごとにHTTP通信する方式にはしない。

5分経過を判定し、必要なタイミングのみ送信する。

## 13. Discordアカウント連携

Minecraft側に以下のコマンドを追加する。

```text
/discord link
```

実行したプレイヤー本人のMinecraft UUIDを取得する。

APIへコード発行を要求する。

```text
POST /v1/minecraft/link-code
```

APIから6桁コードを受け取る。

例

```text
482193
```

Minecraft内に以下のように表示する。

```text
Discord連携コードを発行しました。

482193

Discordで
/link 482193
を実行してください。

このコードは10分間有効です。
```

Minecraft側でDiscord IDを入力させない。

## 14. 連携コード

コード生成はAPI側で行う。

Mod側でランダムコードを生成しない。

複数Minecraftサーバー間でコード重複を防ぐため、API側で一元管理する。

ModはAPIから返されたコードをプレイヤーへ表示するだけとする。

## 15. Config

サーバーごとに設定ファイルを生成する。

概念上の設定内容は以下。

```toml
server_id = "oceanblock2"

api_url = "https://example.workers.dev"

api_key = "mc_xxxxxxxxxxxxxxxxx"
```

API Keyはjarへ埋め込まない。

GitHubにもコミットしない。

## 16. Server ID

各Minecraftサーバーを一意に識別する。

例

```text
oceanblock2
atm
stoneblock
```

Modpack名そのものではなく、API上のサーバー識別子として扱う。

## 17. API通信

すべてHTTPSを使用する。

Minecraft Modから送信するリクエストには以下を付与する。

```http
Authorization: Bearer mc_xxxxxxxxxxxxx
```

API通信はMinecraftのメインスレッドをブロックしない。

## 18. 非同期処理

以下のような構造とする。

```text
Minecraft Main Thread
        │
        │ Event
        ▼
   Event Handler
        │
        ▼
      Queue
        │
        ▼
Background Worker
        │
        ▼
      HTTPS
        │
        ▼
       API
```

API応答待ちによってMinecraftサーバーのTick処理を停止させない。

## 19. API障害時

Cloudflare Workersやネットワークに障害が発生してもMinecraftサーバー自体は継続動作させる。

Join通知送信に失敗しても、プレイヤーのMinecraft参加を妨げない。

エラーはサーバーログへ記録する。

## 20. Timeout

HTTP通信にはTimeoutを設定する。

APIが応答しない状態でWorker Threadが無期限に待機しないようにする。

## 21. Retry

一時的な通信障害に対して限定的なRetryを行える設計とする。

無限Retryは行わない。

Join / Leaveなどのイベントが大量に蓄積してMinecraftサーバーのメモリを圧迫しないようにする。

## 22. Shutdown

Minecraftサーバー停止時はServer StopイベントをAPIへ送信する。

ただしAPI通信完了を長時間待ってMinecraftサーバーのShutdownを妨げない。

## 23. Minecraftサーバーへの負荷

以下を禁止する。

```text
毎Tick HTTP通信
毎秒DB同期
毎Tick全プレイヤー走査
Discord Gatewayへの常時接続
プレイ時間の毎分送信
```

イベント駆動を基本とする。

定期処理は5分ごとのHeartbeatのみとする。

## 24. 参考実装

以下の既存プロジェクトの設計・実装を参考にする。

### SDLink

主に以下を参考にする。

```text
Forgeイベント処理
NeoForgeイベント処理
Player Join / Leave
Server Start / Stop
Loader間のコード分離
Discord連携処理
Whitelist連携
```

### DiscordSRV

主に以下を参考にする。

```text
Minecraft UUIDとDiscord IDの紐づけ
連携コード方式
Discord Role連携
連携状態管理
```

既存実装を確認せず、同等機能を必要以上に独自実装しない。

## 25. 開発順序

最初にForge 1.20.1版を完成させる。

1. Forge 1.20.1開発環境構築
2. Player Join取得
3. Player Leave取得
4. Server Start取得
5. Server Stop取得
6. commonへのイベント受け渡し
7. 非同期HTTP通信
8. API接続
9. Heartbeat
10. /discord link
11. Forge版MVP動作確認

Forge版が安定した後にNeoForge 1.21.1へ対応する。

1. NeoForge 1.21.1開発環境構築
2. Loader固有イベント実装
3. commonへ接続
4. Config実装
5. コマンド実装
6. Forge版と同一APIへ接続
7. 動作比較

## 26. GitHub Actions

ForgeとNeoForgeを別Jobでビルドする。

概念上は以下。

```text
build-forge-1.20.1

build-neoforge-1.21.1
```

両方がビルド可能であることをPull Request時に確認する。

## 27. Release

GitHub Releaseには両方のjarを添付する。

```text
mc-community-forge-1.20.1-0.1.0.jar

mc-community-neoforge-1.21.1-0.1.0.jar
```

## 28. MVPで取得するイベント

```text
Player Join
Player Leave

Server Start
Server Stop

Heartbeat
```

加えて以下を提供する。

```text
/discord link
```

## 29. MVP対象外

初期バージョンでは以下を取得しない。

```text
Death
Mob Kill
Block Break
Block Place
Chat
Advancement
Position
Inventory
```

Minecraftサーバーへの不要な監視処理を増やさない。

## 30. 将来拡張

基盤完成後に必要に応じて以下を追加する。

```text
Modpack別プロフィール
プレイ時間ランキング
貢献度
Discord Role自動付与
サーバー参加権限
Whitelist連携
Webプロフィール
```

これらを追加してもForge / NeoForge固有コードへビジネスロジックを追加せず、可能な限りcommonまたはAPI側へ実装する。